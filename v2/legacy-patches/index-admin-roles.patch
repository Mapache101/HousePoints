*** Begin Patch
*** Update File: index.html
@@
-  const ADMIN_EMAIL = 'dolguin@scis-bo.com';
+  const ADMIN_EMAIL = 'dolguin@scis-bo.com';
+
+  async function isLegacyAdminEmail(email) {
+    const normalized = (email || '').toLowerCase();
+    if (normalized === ADMIN_EMAIL) return true;
+    try {
+      const { db, doc, getDoc, appId } = window.FirebaseDB;
+      const snap = await getDoc(doc(db, 'artifacts', appId, 'public', 'data', 'settings', 'roles'));
+      const admins = (snap.exists() ? snap.data().admins || [] : []).map(e => String(e).toLowerCase());
+      return admins.includes(normalized);
+    } catch (error) {
+      console.warn('Could not read settings/roles for legacy admin check', error);
+      return false;
+    }
+  }
@@
-  {(user.email === ADMIN_EMAIL || user.email?.toLowerCase() === ADMIN_EMAIL) && (
+  {isAdmin && (
@@
-  {view === 'admin' && user && user.email?.toLowerCase() === ADMIN_EMAIL && <Admin />}
+  {view === 'admin' && user && isAdmin && <Admin />}
*** End Patch
